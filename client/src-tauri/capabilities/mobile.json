{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile-vpn",
  "description": "Mobile window capability. Native VPN plugin commands are intentionally absent: Rust retains the private plugin handle so bearer grants never cross JavaScript.",
  "platforms": ["android", "iOS"],
  "windows": ["main"],
  "permissions": []
}
